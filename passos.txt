como fazer com o git bash

abra o terminal o do git, em seguida cd \ (windows) cd ~ (linux)
depois git clone <url> e depois abrir no vs code

comandos 

npx express-generator --no-view <nome projeto>
em seguida cd <nome projeto> e npm install depois
npm i mongose
npm install dotenv -- arquivo a parte para colocar senha, nao sera commitado para o github

criar arquivo na raiz .gitignore
criar arquivo na pasta models das coleções 

criar arquivo .env.sample